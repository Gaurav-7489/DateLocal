# Production readiness pass

- Campus About page is available at `/about`.
- Landing page uses the optimized campus image set with lazy loading.
- OAuth completion is based on `profiles.profile_completed`.
- Public 404 uses the campus-outside image.
- Public navigation avoids the heavier scroll animation dependency.
