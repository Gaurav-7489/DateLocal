"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { Heart, Loader2, MessageCircle } from "lucide-react";
import { likeProfile } from "@/app/(app)/app/discover/actions";
import SuperChatComposer from "@/components/payments/superchat-composer";
import { routes } from "@/config/routes";
export default function ProfileActions({targetUserId,targetName}:{targetUserId:string;targetName:string}){const router=useRouter();const [loading,setLoading]=useState(false);const [message,setMessage]=useState<string|null>(null);const [superChat,setSuperChat]=useState(false);async function like(){if(loading)return;setLoading(true);setMessage(null);try{const result=await likeProfile(targetUserId);if(result.error){setMessage(result.error);return;}if(result.matched&&result.matchId)router.push(`${routes.messages}/${result.matchId}`);else{setMessage("Like sent.");router.refresh();}}finally{setLoading(false);}}return <><div className="flex flex-1 gap-2"><button type="button" onClick={()=>void like()} disabled={loading} className="flex flex-1 items-center justify-center gap-2 rounded-2xl bg-emerald-600 py-3 text-xs font-black text-white active:scale-[.98] disabled:opacity-60">{loading?<Loader2 className="h-4 w-4 animate-spin"/>:<Heart className="h-4 w-4 fill-current"/>}{loading?"Sending…":"Like"}</button><button type="button" onClick={()=>setSuperChat(true)} className="flex flex-1 items-center justify-center gap-2 rounded-2xl bg-violet-600 py-3 text-xs font-black text-white active:scale-[.98]"><MessageCircle className="h-4 w-4"/>SuperChat</button></div>{message&&<p className="mt-2 text-center text-[9px] font-semibold text-emerald-700">{message}</p>}{superChat&&<SuperChatComposer targetUserId={targetUserId} targetName={targetName} onClose={()=>setSuperChat(false)}/>}</>}
