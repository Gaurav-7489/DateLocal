import type { Metadata } from "next";import Link from "next/link";import Image from "next/image";import { notFound, redirect } from "next/navigation";import { ArrowLeft, ShieldCheck, MapPin, UserRound } from "lucide-react";import { createServerSupabaseClient } from "@/lib/supabase/server";import { routes } from "@/config/routes";import { getProfilePhotoUrl } from "@/lib/profile-photo";import { calculateAge } from "@/lib/utils";import ProfileActions from "@/components/profile/profile-actions";
type Props={params:Promise<{userId:string}>};type ProfileRow={id:string;display_name:string;date_of_birth:string;gender:string;department:string;academic_year:string;bio:string|null;profile_completed:boolean;ghost_mode:boolean;campus_residency:string|null;relationship_goal:string|null;zodiac:string|null;sleep_habit:string|null;caffeine_pref:string|null;weekend_vibe:string|null;prompt_question:string|null;prompt_answer:string|null;profile_photos:{id:string;storage_path:string;is_primary:boolean;display_order:number}[];profile_interests:{id:string;name:string}[]};
export const metadata:Metadata={title:"Extrovert Date Profile"};export const dynamic="force-dynamic";function Badge({ok,label}:{ok:boolean;label:string}){return <span className={`inline-flex items-center gap-1 rounded-full border px-2.5 py-1 text-[9px] font-black ${ok?"border-emerald-300/40 bg-emerald-500/90 text-white":"border-rose-300/40 bg-rose-500/90 text-white"}`}><ShieldCheck className="h-2.5 w-2.5"/>{label}</span>}
export default async function StudentProfilePage({params}:Props){const {userId}=await params;const supabase=await createServerSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect(routes.login);if(user.id===userId)redirect(routes.profile);const {data:rows,error}=await supabase.rpc("get_student_profile",{p_user_id:userId});if(error)notFound();const profile=(rows?.[0]??null) as ProfileRow|null;if(!profile||profile.ghost_mode)notFound();const [{data:viewer},{data:trust}]=await Promise.all([supabase.from("profiles").select("ghost_mode").eq("id",user.id).maybeSingle(),supabase.from("extrovert_profiles").select("verification_status,area_verification_status,area_id,profile_photo_path,identity_type,institution_name,field_of_study,job_title,employer_name,role_description").eq("id",userId).maybeSingle()]);let areaName:string|null=null;if(trust?.area_id){const {data:area}=await supabase.from("extrovert_areas").select("name").eq("id",trust.area_id).maybeSingle();areaName=area?.name??null}if(!viewer?.ghost_mode){try{await supabase.from("profile_views").insert({viewer_id:user.id,viewed_id:userId})}catch{}}const photos=[...(profile.profile_photos??[])].sort((a,b)=>Number(b.is_primary)-Number(a.is_primary)||a.display_order-b.display_order);const photoUrls=photos.map(p=>getProfilePhotoUrl(p.storage_path,640)).filter(Boolean) as string[];const sharedPhoto=getProfilePhotoUrl(trust?.profile_photo_path,640);const age=calculateAge(profile.date_of_birth);const identityVerified=trust?.verification_status==="verified";const areaVerified=trust?.area_verification_status==="verified";return <main className="mx-auto max-w-md space-y-3.5 px-3.5 py-4 pb-28 font-sans"><div className="flex items-center gap-3 px-1"><Link href={routes.discover} className="flex h-9 w-9 items-center justify-center rounded-full border border-border bg-card text-muted-foreground shadow-xs active:scale-95" aria-label="Back"><ArrowLeft className="h-4 w-4"/></Link><div className="min-w-0"><p className="text-[10px] font-extrabold uppercase tracking-wider text-pink-600">Extrovert Date</p><h1 className="truncate text-lg font-black text-foreground">{profile.display_name||"Profile"}</h1></div></div><section className="overflow-hidden rounded-[2rem] border border-border bg-zinc-950 shadow-xl"><div className="relative aspect-[4/5]">{photoUrls[0]||sharedPhoto?<Image src={photoUrls[0]||sharedPhoto!} alt={profile.display_name} fill priority className="object-cover" sizes="(max-width:640px) 100vw,420px"/>:<div className="flex h-full items-center justify-center bg-gradient-to-br from-pink-600 to-violet-800 text-7xl font-black text-white">{profile.display_name?.charAt(0)??"?"}</div>}<div className="absolute inset-0 bg-gradient-to-b from-black/60 via-transparent to-black/95"/><div className="absolute left-4 right-4 top-4 flex flex-wrap gap-1.5"><Badge ok={identityVerified} label={identityVerified?"Identity verified":"Identity not verified"}/><Badge ok={areaVerified} label={areaVerified?"Area verified":"Area not verified"}/></div><div className="absolute bottom-4 left-4 right-4 text-white"><div className="flex flex-wrap items-baseline gap-2"><h2 className="text-3xl font-black tracking-tight">{profile.display_name}{age!==null?`, ${age}`:""}</h2></div><div className="mt-1 flex flex-wrap items-center gap-2 text-xs font-medium text-zinc-200">{profile.gender&&<span>{profile.gender}</span>}{areaName&&<span className="inline-flex items-center gap-0.5"><MapPin className="h-3 w-3"/>{areaName}</span>}{profile.academic_year&&<span>• {profile.academic_year}</span>}</div>{profile.department&&<p className="mt-1 text-xs text-zinc-300">{profile.department}</p>}</div></div></section>{photoUrls.length>1&&<div className="grid grid-cols-4 gap-2">{photoUrls.slice(1,5).map((url,index)=><div key={url} className="relative aspect-[4/5] overflow-hidden rounded-2xl border border-border bg-zinc-950"><Image src={url} alt={`Photo ${index+2}`} fill className="object-cover" sizes="100px"/></div>)}</div>}{profile.bio&&<section className="rounded-3xl border border-border bg-card p-4"><p className="text-sm leading-relaxed text-foreground">“{profile.bio}”</p></section>}{profile.prompt_question&&profile.prompt_answer&&<section className="rounded-3xl border border-pink-100 bg-pink-50/50 p-4"><p className="text-[10px] font-extrabold uppercase tracking-wider text-pink-700">{profile.prompt_question}</p><p className="mt-1 text-sm font-semibold leading-relaxed text-foreground">“{profile.prompt_answer}”</p></section>}<section className="rounded-3xl border border-border bg-card p-4"><p className="text-[10px] font-extrabold uppercase tracking-wider text-muted-foreground">Identity & vibe</p><div className="mt-3 grid grid-cols-2 gap-2"><div className="rounded-2xl bg-muted/40 p-3"><p className="text-[9px] uppercase font-bold text-muted-foreground">Gender</p><p className="mt-1 text-xs font-black capitalize">{profile.gender||"Not set"}</p></div><div className="rounded-2xl bg-muted/40 p-3"><p className="text-[9px] uppercase font-bold text-muted-foreground">Area</p><p className="mt-1 text-xs font-black">{areaName||"Not set"}</p></div></div><div className="mt-3 flex flex-wrap gap-1.5">{[profile.relationship_goal,profile.zodiac,profile.sleep_habit,profile.caffeine_pref,profile.weekend_vibe,...(profile.profile_interests??[]).map(i=>i.name)].filter(Boolean).map(item=><span key={String(item)} className="rounded-full border border-border bg-muted/60 px-2.5 py-1 text-[11px] font-semibold text-foreground">{item}</span>)}</div></section><div className="sticky bottom-[calc(4.75rem+env(safe-area-inset-bottom))] z-30 flex items-center gap-2 rounded-3xl border border-border/80 bg-background/90 p-2 shadow-xl backdrop-blur-xl"><Link href={routes.discover} className="flex h-11 w-11 shrink-0 items-center justify-center rounded-2xl border border-border bg-card text-muted-foreground active:scale-95" aria-label="Back to Discover"><ArrowLeft className="h-4 w-4"/></Link><ProfileActions targetUserId={profile.id} targetName={profile.display_name}/><Link href={routes.profile} className="flex h-11 w-11 shrink-0 items-center justify-center rounded-2xl border border-border bg-card text-muted-foreground active:scale-95" aria-label="My profile"><UserRound className="h-4 w-4"/></Link></div></main>}
